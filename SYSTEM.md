# SYSTEM.md — 金属材料计算器 PWA

## 项目目标

提供一个可在 iPhone / 安卓 / 鸿蒙主屏幕安装、离线使用的金属材料重量计算器：

- 输入型材形状、尺寸与材料，实时计算体积、单件重量、每米重量、总重与金额。
- 纯静态 Web 应用（PWA），无需后端、无需 Apple 开发者账号即可安装使用。
- 本项目面向 Windows 环境开发，免费托管于 GitHub Pages。

## 功能模块划分

| 模块 | 文件 | 职责 |
|---|---|---|
| 数据定义 | `js/materials.js` | 16 种金属材料密度、9 种型材公式与参数定义（纯数据） |
| 查表数据 | `js/tables.js` | 槽钢 / 工字钢国标理论重量表（GB/T 706，kg/m） |
| 计算逻辑 | `js/calc.js` | 单位换算、体积/重量/金额计算、数字格式化（纯函数，可单测） |
| 页面交互 | `js/app.js` | DOM 渲染、事件绑定、结果刷新、复制结果 |
| 样式 | `css/base.css`、`css/form.css`、`css/result.css` | 工业蓝本风格（基础 / 表单 / 结果三层拆分） |
| 页面结构 | `index.html` | 页面骨架与静态资源引用 |
| PWA 配置 | `manifest.json`、`sw.js` | 主屏幕安装元数据、离线缓存 |
| 图标 | `assets/*.png` | 主屏幕图标（180/192/512/1024） |
| 测试 | `tests/calc.test.js` | 计算逻辑单元测试 |
| 文档 | `SYSTEM.md`、`README.md` | 系统说明与使用 / 部署文档 |

## 系统整体架构

纯前端单页应用，无构建步骤、无运行时依赖：

```
index.html
  └─ js/app.js（UI 层）
       └─ js/calc.js（计算层）
            ├─ js/materials.js（材料与型材定义）
            └─ js/tables.js（国标查表数据）
  └─ css/（base / form / result）
  └─ manifest.json / sw.js（PWA 层）
```

`calc.js` 不依赖 DOM 与浏览器 API，可在 Node 中直接单元测试。

## 核心流程

1. 页面加载：`app.js` 渲染材料 / 型材下拉、尺寸输入框，执行首次计算。
2. 用户切换材料（含自定义密度）、型材（含查表型号）、单位或修改尺寸 / 数量 / 单价 → `calcAll()` 重新计算 → 刷新结果面板。
3. 尺寸单位切换时按比例换算已填数值（mm/cm/m），不会重新解读数字。
4. Service Worker 首次访问时预缓存全部静态资源，之后离线可用（缓存优先 + 后台刷新）。

## 关键业务逻辑

### 计算规则

- 所有尺寸统一换算为 cm 参与体积计算（`mm→0.1`、`cm→1`、`m→100`）。
- 体积 `cm³ × 密度 g/cm³ ÷ 1000 = 重量 kg`。
- 每米重量 = 单件重量 ÷ 长度（m）。
- 金额 = 总重 × 单价（¥/kg），单价留空则不显示金额。
- 自定义材料：材料下拉选择「自定义材料」后输入密度（g/cm³），必须为大于 0 的数字。
- 查表型材（槽钢 / 工字钢）：重量 = 国标理论重量(kg/m) × 长度(m)，体积按重量与密度反推。

### 型材公式

| 型材 | 体积公式 |
|---|---|
| 板材 | `V = t × w × l` |
| 圆棒 | `V = π/4 × d² × l` |
| 方棒 | `V = a² × l` |
| 六角棒 | `V = (√3/2) × s² × l`（s = 对边距） |
| 圆管 | `V = π/4 × (D² − (D−2t)²) × l` |
| 方管 | `V = (a² − (a−2t)²) × l` |
| 角钢 | `V ≈ (A+B−t) × t × l`（行业常用近似式） |
| 槽钢 / 工字钢 | 查表：重量 = 国标理论重量 × 长度（型号下拉选择） |

### 校验规则

- 所有尺寸必须为大于 0 的数字。
- 圆管 / 方管：壁厚必须小于外径 / 外边长的 1/2。
- 角钢：壁厚必须小于两个边长。
- 槽钢 / 工字钢：必须选择型号；自定义密度必须为大于 0 的数字。
- 非法输入在结果区显示错误提示，不输出结果。

## 数据流 / 状态流

- 交互状态集中在 `app.js` 的 `state` 对象（材料、型材、单位、自定义密度、数量、单价、按型材分别保存的尺寸）。
- 每次输入变化 → 读取 `state` → 调 `calcAll()` → 返回结构化结果 → 渲染结果网格。
- 尺寸按型材分别保存（`dimsByShape`），切换型材不丢失已填数据；单位切换时对所有已存尺寸统一换算。
- 查表型材的 `spec`（型号）为字符串，随 `dimsByShape` 保存，不参与数字换算。

## 已完成内容

- 9 种型材（板材、圆棒、方棒、六角棒、圆管、方管、角钢、槽钢、工字钢）的重量计算。
- 16 种材料 + 自定义密度输入。
- mm/cm/m 尺寸单位切换（自动换算数值）、kg/g/t 结果单位切换。
- 数量合计与单价金额估算。
- 公式提示、错误提示、结果一键复制。
- PWA：manifest、图标、Service Worker 离线缓存。
- 计算逻辑单元测试（`tests/calc.test.js`）。
- 免费部署至 GitHub Pages，iPhone 可直接「添加到主屏幕」使用。

## 尚未完成内容

- 历史记录 / 结果收藏。
- 多语言界面。
- 更多查表型材（如钢轨、H 型钢）或自定义截面积输入。

## 已知问题

- 角钢采用近似公式，与国标理论重量存在 1% 内偏差（工程估算可接受）。
- 查表数据为 GB/T 706 常用型号（槽钢 29 个、工字钢 30 个），特殊非标型号请以厂家手册为准。
- 结果复制依赖剪贴板 API，`http://` 局域网访问时可能回退到 `execCommand` 方案。
- 图标由脚本生成，非专业设计稿，后续可替换为设计版本。

## 特殊设计决策

- 选择 PWA 而非原生 App：Windows 环境无法运行 Xcode，PWA 零注册零成本即可装到手机。
- 计算逻辑与 UI 完全分离：`calc.js` 无 DOM 依赖，便于扩展其他前端或增加测试。
- 查表数据独立为 `js/tables.js`，后续增加其他标准型材只需扩展数据表。
- 下拉选择使用系统原生渲染，避免旧 WebView 内核的 `appearance: none` 兼容问题。
- 不引入构建工具与 npm 依赖：保持零安装、可直接托管的静态目录。

## 部署记录

- 2026-08-12：已部署至 GitHub Pages，线上地址：https://gdjoss.github.io/metal-material-calculator/
- 部署方式说明：本机网络可访问 `api.github.com`，但直连 `github.com` 被网络拦截，`git push` 不可用；改用 GitHub Data API + Contents API 完成上传（先经 Contents API 创建种子提交，再批量 blob → tree → commit → 更新 main 分支）。

## 重要重构纪录

- v1.0（2026-08-12）：初始版本，完成全部基础功能与 PWA 配置。
- v1.1（2026-08-12）：材料 / 型材由按钮组改为下拉选择；单位切换时自动换算已填尺寸；超长数字自动缩小字号并允许换行。
- v1.2（2026-08-12）：移除 `<select>` 的 `appearance: none`，恢复系统原生渲染，修复鸿蒙 / 华为浏览器等旧内核点击下拉不弹列表的问题。
- v1.3（2026-08-12）：新增自定义材料密度输入；新增槽钢 / 工字钢国标查表计算（新增 `js/tables.js`，计算逻辑支持 `type: select` 参数与查表型材）。
- v1.4（2026-08-12）：自定义材料选中后，材料下拉的显示文字会实时显示当前密度（如「自定义材料（6.9 g/cm³）」），未填时显示「待输入密度」，避免混淆实际计算密度。
- v1.5（2026-08-12）：修复自定义密度输入框的显示控制——`.field` 的 `display: flex` 覆盖了 `hidden` 属性的默认隐藏效果，导致正常材料时输入框仍可见；新增 `[hidden] { display: none !important; }` 全局规则，确保只有选择「自定义材料」时输入框才显示。
- v1.6（2026-08-12）：进一步加固隐藏逻辑——JS 直接设置内联 `display: none`，彻底绕过浏览器 CSS 兼容差异；Service Worker 对页面导航改为网络优先（在线时总是拿到最新版，离线才用缓存）；页面底部新增版本号便于确认更新状态。
- v1.7（2026-08-12）：修复"页面新版 + JS 旧缓存"的不同步问题——Service Worker 改为对所有资源网络优先（在线必拿最新文件，离线回退缓存）；版本号改由 `app.js` 渲染（看到 v1.7 即证明新 JS 已运行，输入框隐藏逻辑必然生效）。
- v1.8（2026-08-12）：新 Service Worker 安装完成后通过 `SKIP_WAITING` 消息立即激活，页面监听 `controllerchange` 自动刷新一次，用户无需手动开两次即可拿到最新版本。
